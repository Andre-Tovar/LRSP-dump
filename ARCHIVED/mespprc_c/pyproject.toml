[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mespprc_c"
version = "0.1.0"
description = "C-translated MESPPRC research prototype package"
requires-python = ">=3.11"
dependencies = [
  "pulp>=3.3",
]
