matplotlib>=3.7
numpy>=1.24
# Upper-bounded: the LRSP master in lrsp_solver/master_problem.py uses the
# pre-4.0 PuLP API (direct LpVariable construction, constraints-as-dict),
# which PuLP 4.0 removes.
pulp>=3.3,<4
pytest>=7.0
